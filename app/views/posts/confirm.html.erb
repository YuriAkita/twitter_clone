<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h3>以下の内容で、送信する。</h3>
    <%= form_with(model: @post, local: true) do |form| %>
      <%= form.hidden_field :content %>
      <p>内容:<%= @post.content %></p>
      <%= form.submit  "登録する" %>
    <% end %>
  </body>
</html>
